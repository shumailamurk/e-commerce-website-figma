/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['sdn.sanity.io', 'cdn.sanity.io'], // Add both 'sdn.sanity.io' and 'cdn.sanity.io'
    },
  };
  
  export default nextConfig;
  